/** Breakpoint (px) for desktop two-column home layout (see `lg:` usage in app). */
export const HOME_DESKTOP_MIN_WIDTH_PX = 1224;

/** Tailwind padding bottom to clear fixed mobile nav — keep in sync with nav height. */
export const HOME_MOBILE_BOTTOM_NAV_PADDING_CLASS = 'pb-[110px]' as const;

export const HOME_EXPLORE_CARD_LIMIT = 6;
export const HOME_BY_LEVEL_CARD_LIMIT = 3;
